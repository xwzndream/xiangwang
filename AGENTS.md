# 项目开发规范（AI / Codex 执行指南）

## 技术选型（必须遵守）
- 前端框架：React（优先）
- 样式方案：Tailwind CSS
- 状态管理：useState（简单场景）或 Zustand（推荐）
- 项目类型：单页 Landing Page（SPA）
- 部署平台：Netlify
- 线索存储：Twikoo（轻量后端）
- 通知方式：邮件提醒 + 微信提醒
- 服务端中转：Netlify Functions
- 我的联系方式：17521217112 手机/微信

--------------------------------------------------

## 核心功能（必须实现）

### 1. 报价计算器
- 支持多选（checkbox）
- 实时计算价格
- 自动计算预付（50%）
- 显示：合计 / 预付金额

### 2. 弹窗咨询流程（Modal Step）
必须包含 4 步：
1. 选择服务类型
2. 勾选服务范围
3. 显示报价结果
4. 填写联系方式

要求：
- 每一步可前进/返回
- 数据在步骤间保持
- 实时更新价格

### 3. 联系方式收集
- 仅保留一个必填字段：电话或微信
- 降低用户流失

### 4. CTA 按钮（关键）
页面必须包含：
- 首屏按钮
- 报价区按钮
- 底部按钮
- 右下角悬浮按钮（固定）

按钮文案统一：
👉 立即获取方案与报价

### 5. 表单提交与通知
必须实现：
- 表单提交后写入线索数据
- 成功后触发邮件提醒
- 成功后触发微信提醒
- 前端显示提交成功状态

要求：
- 提交逻辑通过 Netlify Functions 中转
- 不允许前端直接暴露敏感通知密钥
- 必须保留后续升级为正式后端的扩展能力

--------------------------------------------------

## UI 实现规范

### 页面风格
- 白底
- 深色文字
- 卡片式布局
- 间距充足（padding >= 24px）

### 按钮
- 主按钮必须突出（黑色或蓝色）
- hover 有轻微变化

### 布局
- 最大宽度：1200px
- 居中
- 移动端优先

--------------------------------------------------

## 组件拆分（必须按此结构实现）

/components
- Hero.tsx（首屏）
- Trust.tsx（信任背书）
- Services.tsx（服务范围）
- Pricing.tsx（报价引导）
- Calculator.tsx（报价计算器）
- Process.tsx（流程）
- Rules.tsx（规则）
- CTA.tsx（最终转化区）
- FloatingButton.tsx（悬浮按钮）
- ModalFlow.tsx（弹窗流程）
- ContactForm.tsx（联系方式）

--------------------------------------------------

## 数据结构（报价）

使用 JSON 配置：

{
  "client": [
    { "name": "Android", "price": 10000 },
    { "name": "iOS", "price": 10000 },
    { "name": "Web", "price": 10000 }
  ],
  "server": [
    { "name": "API", "price": 10000 },
    { "name": "Admin", "price": 10000 }
  ],
  "extra": [
    { "name": "Deploy", "price": 5000 }
  ]
}

--------------------------------------------------

## 交互要求（重点）

- 所有价格必须实时更新
- 勾选即变化（无刷新）
- 弹窗流畅切换（无卡顿）
- 移动端可单手操作
- 表单提交成功后必须有明确反馈
- 用户提交后必须触发消息通知，避免漏单

--------------------------------------------------

## 禁止事项

- ❌ 不允许使用复杂路由
- ❌ 不允许多页面跳转
- ❌ 不允许强制用户填写过多信息
- ❌ 不允许隐藏价格逻辑

--------------------------------------------------

## 目标

这是一个“高转化接单页面”，不是普通官网。

优先级：
1. 转化率
2. 交互体验
3. 视觉美观

不是：
- 复杂功能
- 炫技动画

--------------------------------------------------

## 技术建议：

- React / Vue（推荐 React）
- Tailwind CSS（快速构建 UI）
- 状态管理：useState / Zustand
- 模块化组件结构
- 部署托管：Netlify
- 轻量后端：Twikoo（用于线索存储）
- 消息通知：邮件提醒 + 微信提醒
- 推荐架构：Netlify 托管前端，表单提交通过函数中转后写入 Twikoo，并同步发送邮件 / 微信通知

--------------------------------------------------

## 核心功能：

- 报价计算器（支持多选 + 实时计算）
- 智能咨询弹窗（Step 分步骤引导）
- 联系方式收集（简化字段）
- 多处 CTA 按钮（提升点击率）
- 右下角悬浮按钮（全局入口）
- 表单线索存储（Twikoo / 轻量后端）
- 提交通知提醒（邮件 / 微信）

--------------------------------------------------

## 页面结构说明：

- 所有模块按“转化漏斗”排列
- 每一屏都有明确目标（展示 / 引导 / 转化）
- 页面中至少出现 2-3 次主 CTA 按钮
- 重要信息（价格 / 服务）尽量前置
- 表单提交后需要有成功反馈与通知触发
- 所有线索需要可存储、可查看、可追踪
- 后端方案优先低成本、可快速上线

--------------------------------------------------

9. 部署与后端方案（低成本上线版）
标题：部署与消息通知

前端部署：
- 使用 Netlify 托管前端页面
- 支持 Git 自动部署
- 自动 HTTPS
- 适合作为单页 Landing Page 上线方案

后端与数据存储：
- 使用 Twikoo 作为轻量后端存储线索数据
- 适用于 MVP 阶段的表单留资
- 当前阶段仅需存储用户联系方式、勾选服务、报价结果、留言信息

推荐提交流程：
1. 用户填写报价表单并提交
2. 前端调用 Netlify Function（或等价服务端函数）
3. 服务端函数执行以下动作：
   - 写入 Twikoo
   - 发送邮件提醒
   - 发送微信提醒
4. 前端展示“提交成功”提示

通知方案：
- 邮件提醒：作为兜底通知方式
- 微信提醒：作为即时通知方式
- 推荐两者同时启用，避免漏单

字段建议：
- 联系方式（电话 / 微信）
- 服务类型
- 服务范围
- 合计金额
- 预付金额
- 留言内容
- 提交时间

说明：
- 该方案适合个人接单场景
- 优先目标是快速上线与低成本运行
- 后续如线索增长，可升级为 Supabase / Firebase / 自建后端

--------------------------------------------------